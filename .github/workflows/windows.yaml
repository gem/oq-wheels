name: Build Geospatial Wheels (Windows)

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  build-windows-wheels:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      # 1. Configura l'ambiente Python
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          
# 2. Installa OSGeo4W e VERIFICA l'installazione
      - name: Install OSGeo4W (GDAL, PROJ & Devel files)
        shell: powershell
        run: |
          echo "Downloading OSGeo4W installer..."
          Invoke-WebRequest -Uri "https://download.osgeo.org/osgeo4w/osgeo4w-setup.exe" -OutFile "osgeo4w-setup.exe"
          
          # Creazione della cartella temporanea per i pacchetti scaricati
          New-Item -ItemType Directory -Force -Path "C:\OSGeo4W-Packages"

          echo "Running OSGeo4W installer..."
          # Usiamo i flag corretti per forzare il download e l'estrazione da internet in modo silenzioso
          Start-Process -FilePath ".\osgeo4w-setup.exe" -ArgumentList `
            "--quiet-mode", `
            "--advanced", `
            "--autoaccept", `
            "--no-shortcuts", `
            "--no-desktop", `
            "--no-startmenu", `
            "--root", "C:\OSGeo4W", `
            "--local-package-dir", "C:\OSGeo4W-Packages", `
            "--site", "https://download.osgeo.org/osgeo4w/v2", `
            "--packages", "gdal,gdal-devel,proj,proj-devel" `
            -Wait -NoNewWindow

          echo "Verifying installation paths..."
          if (Test-Path "C:\OSGeo4W\include\gdal.h") {
              Write-Host "Success: gdal.h found at C:\OSGeo4W\include\gdal.h" -ForegroundColor Green
          } else {
              Write-Error "CRITICAL ERROR: OSGeo4W installation failed! C:\OSGeo4W\include\gdal.h does not exist."
              # Mostra cosa è stato effettivamente estratto per debuggare se fallisce ancora
              Get-ChildItem -Path "C:\OSGeo4W" -Recurse -Depth 2 -ErrorAction SilentlyContinue
              exit 1
          }
          
      # 3. Installazione dei prerequisiti di compilazione
      - name: Install Build Tools & Compilation Requirements
        shell: powershell
        run: |
          python -m pip install --upgrade pip setuptools wheel delvewheel
          python -m pip install cython numpy==2.2.6 versioneer packaging
          
# 4. Compila le CINQUE wheel dai sorgenti
      - name: Build Unrepaired Wheels (GDAL, Fiona, PyProj, Rasterio & PyOgrio)
        shell: powershell
        run: |
          # PATH e directory di PROJ indispensabili per pyproj/rasterio
          [System.Environment]::SetEnvironmentVariable("PATH", "C:\OSGeo4W\bin;" + [System.Environment]::GetEnvironmentVariable("PATH", "Process"), "Process")
          $env:PROJ_DIR = "C:\OSGeo4W"

          # Variabili d'ambiente richieste per la retrocompatibilità di Fiona/PyOgrio
          $env:GDAL_VERSION = "3.13.1"
          [System.Environment]::SetEnvironmentVariable("GDAL_VERSION", "3.13.1", "Process")
          $env:GDAL_HOME = "C:\OSGeo4W"
          $env:GDAL_INCLUDE_PATH = "C:\OSGeo4W\include"
          $env:GDAL_LIBRARY_PATH = "C:\OSGeo4W\lib"

          New-Item -ItemType Directory -Force -Path .\dist_unrepaired

          # Stringhe di configurazione esplicite per il compilatore C++ (MSVC)
          # Questo passa i percorsi direttamente a setuptools/build backend saltando l'isolamento di pip
          $build_opts = '--config-settings="--global-option=build_ext" --config-settings="--global-option=-IC:\OSGeo4W\include" --config-settings="--global-option=-LC:\OSGeo4W\lib"'

          echo "Compilazione GDAL..."
          Invoke-Expression "pip wheel GDAL==3.13.1 --no-build-isolation --wheel-dir=.\dist_unrepaired $build_opts"

          echo "Compilazione Fiona..."
          Invoke-Expression "pip wheel fiona --no-binary fiona --no-build-isolation --wheel-dir=.\dist_unrepaired $build_opts"

          echo "Compilazione PyProj..."
          Invoke-Expression "pip wheel pyproj --no-binary pyproj --no-build-isolation --wheel-dir=.\dist_unrepaired $build_opts"

          echo "Compilazione Rasterio..."
          Invoke-Expression "pip wheel rasterio --no-binary rasterio --no-build-isolation --wheel-dir=.\dist_unrepaired $build_opts"

          echo "Compilazione PyOgrio..."
          Invoke-Expression "pip wheel pyogrio --no-binary pyogrio --no-build-isolation --wheel-dir=.\dist_unrepaired $build_opts"
